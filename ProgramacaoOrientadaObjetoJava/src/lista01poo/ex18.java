package lista01poo;
import javax.swing.JOptionPane;/*
public class ex18 {

			public static void main(String[] args) {
				
				EstruturaLivro livro[]= new EstruturaLivro [5];
				
				for(int i=0; i<livro.length; i++) { //inserindo dados no registro
					
					livro[i]= new EstruturaLivro ();
					livro[i].titulo = JOptionPane.showInputDialog("insira o titulo do livro: ");
					livro[i].anoEdicao = Integer.parseInt(JOptionPane.showInputDialog("insira o ano de edição: "));
					livro[i].numPaginas = Integer.parseInt(JOptionPane.showInputDialog("insira o número de páginas: "));
					livro[i].preco = Double.parseDouble(JOptionPane.showInputDialog("insira o preço do livro: "));
				}
				for (int i=0; i<livro.length; i++) { //lendo dados do registro
					System.out.println("título: " + livro[i].titulo + "- ano de edição " + livro[i].anoEdicao + 
					                   "- número de páginas: " + livro[i].numPaginas + "- preço: R$ " + livro[i].preco);
				}
	}

}
*/